/**
 * Global test setup for Orbit plugin tests.
 *
 * This file runs before every test file. The jsdom environment
 * provides document/window globals. Additional polyfills can be
 * added here if needed.
 */

// Ensure any global setup needed for jsdom environment
// (jsdom provides document, window, etc. automatically)
